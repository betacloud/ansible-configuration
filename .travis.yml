---
sudo: required

language: python

before_install:
  - openssl aes-256-cbc -K $encrypted_0b31f6c9f925_key -iv $encrypted_0b31f6c9f925_iv -in molecule/default/clouds.yml.enc -out molecule/default/clouds.yml -d

install:
  - pip install -r test-requirements.txt

script:
  - tox

notifications:
  irc:
    channels:
      - "irc.freenode.org#osism"
    use_notice: true
